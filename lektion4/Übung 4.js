function artikel_Gekauft() {
    let artikelName = "Streukäse"
    let artikelGekauft = true

    if(artikelgekauft ==true){
        console.debug("[Artikel]") \"" + "\" wurde gekauft")
    }
}

function artikel_Hinzufügen(){
    gruppenname = "Getreide"
    let artieklName = "Quinoa"
    if(artikelNam.length >0) {
        console.debug("[" + gruppenName + "] \ "" + artikelName + "\" hinzugefügt")
    }
}

function gruppe_Hinzufügen(){
    let gleicheGruppen = []
    let neueGruppe = "Getränke"
    if (gleicheGruppen.lenght == 0){
        console.debug("[App] Gruppe \ "")
    }
}